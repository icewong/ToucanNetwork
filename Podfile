# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ToucanNetwork' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  #pod 'XmlJson'
  pod 'XMLDictionary'
  pod 'ISO8601'
  pod 'CryptoSwift'
  # Pods for ToucanNetwork

  target 'ToucanNetworkTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ToucanNetworkUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
